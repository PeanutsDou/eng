# WorldBox pipeline — one frag shader per line
# Uses screen.vert as vertex shader for all passes
# "post:" prefix = post-processing pass
# "sprite:" prefix = sprite layer (C++ loads data/<name>.conf)

terrain.frag
clouds.frag
sprite:sprites
post:post.frag
